stages:
  preprocess:
    python: stages.preprocess
    deps:
      raw: data/raw.csv
    outs:
      clean: data/clean.csv

  train:
    python: stages.train
    deps:
      clean: data/clean.csv
    outs:
      model: models/model.pkl
